import React from 'react'

const SectionHeading = ({headingText}) => {
  return (
    <div className='section-heading-container'>
        <h2 className='container-label'>{headingText}</h2>
    </div>
  )
}

export default SectionHeading